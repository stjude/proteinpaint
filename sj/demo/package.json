{
  "name": "test",
  "version": "1.0.0",
  "description": "An example project that uses the PP front and server packages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "npm link ../../front && npm link ../../server",
    "postinstall": "proteinpaint-front",
    "start": "proteinpaint-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
