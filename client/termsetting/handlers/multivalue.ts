// import { getPillNameDefault, set_hiddenvalues } from '../utils.ts'
// import type { QualQ, CategoricalTW, VocabApi } from '#types'
// import { copyMerge } from '#rx'

export function fillTW(/*tw: CategoricalTW, vocabApi: VocabApi, defaultQ: QualQ | null = null*/) {
	// for now, a multivalue tw is expected to not be missing any properties
}
