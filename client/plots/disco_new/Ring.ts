class Ring {
    private radius: number;
    private elements: Array<PlotElement>
    constructor(radius: number, elements: Array<PlotElement>) {
        this.radius = radius;
        this.elements = elements
    }
}