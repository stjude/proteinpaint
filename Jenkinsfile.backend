#!/usr/bin/env groovy

library identifier: "jenkins-lib@master"
dockerPipeline {
    // This repo doesn't use Gitflow, so don't try to parse tag names,
    // and definitely don't create tags automatically.
    tagSource = "branch"
    pushTagsBranches = "match_nothing"

    // Skip tests for now.
    // TODO Setup docker-compose file for this purpose.
    testBranches = "match_nothing"
}
