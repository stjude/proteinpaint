# Please add dependencies in alphabetical order and include version numbers.
# Use 'pip list' to get the version numbers of installed packages on your system.
blitzgsea==1.3.47
biopython==1.87
h5py==3.15.1
httpx==0.28.1
# 3.10.5 is the floor: Path.__deepcopy__ recursed forever on python 3.14 before it
# (matplotlib #29157, fixed by #30198), which killed every savefig -- gsea.py's pathway
# image among them. the container's python3 apt pin is commented out in
# container/deps/Dockerfile, so the base floated 3.13 -> 3.14 and this surfaced in
# production only. 3.10.3 also has no cp314 wheel and was being built from source.
matplotlib==3.10.7
nibabel==5.3.2
numba==0.66.0
numpy>=1.22
openslide-bin==4.0.1.2
openslide-python==1.4.6
pandas==2.2.3
pillow==12.3.0
psutil==7.2.1
pyBigWig==0.3.25 --no-binary=pyBigWig
pydantic==2.12.3
statsmodels==0.14.4
tifffile==2026.7.31
typing_extensions==4.15.0
xlsxwriter==3.2.5
