apiVersion: v1
kind: Service
metadata:
  name: {{ include "proteinpaint.fullname" . }}-svc
  labels:
    {{- include "proteinpaint.labels" . | nindent 4 }}
  {{- if .Values.service.annotations }}
  annotations:
    {{- toYaml .Values.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "proteinpaint.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: 3456
    - name: https
      port: 443
      targetPort: 3456
