#!/bin/sh

set -e

BRANCH=$(git rev-parse --abbrev-ref HEAD)
FILES=$(git diff-tree --no-commit-id --name-only -r HEAD..origin/$BRANCH)

# doing per GDC requirements
pre-commit run --files "$FILES"

exit 0
